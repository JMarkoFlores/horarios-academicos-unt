services:
  postgres:
    image: postgres:16-alpine
    container_name: ${POSTGRES_CONTAINER_NAME}
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - horarios_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: ${PGADMIN_CONTAINER_NAME}
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "${PGADMIN_PORT}:80"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - horarios_network
    volumes:
      - pgadmin_data:/var/lib/pgadmin

  redis:
    image: redis:7-alpine
    container_name: ${REDIS_CONTAINER_NAME}
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - redis_data:/data
    networks:
      - horarios_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 15s
      timeout: 5s
      retries: 5

  #  backend:
  #    build:
  #      context: ./backend
  #      dockerfile: Dockerfile
  #    container_name: ${BACKEND_CONTAINER_NAME}
  #    environment:
  #      DATABASE_HOST: postgres
  #      DATABASE_PORT: 5432
  #      DATABASE_NAME: ${POSTGRES_DB}
  #      DATABASE_USER: ${POSTGRES_USER}
  #      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
  #      DATABASE_SSL: "false"
  #      JWT_SECRET: ${JWT_SECRET}
  #      JWT_EXPIRACION: ${JWT_EXPIRACION}
  #      REDIS_HOST: redis
  #      REDIS_PORT: 6379
  #      REDIS_TTL: ${REDIS_TTL}
  #      NODE_ENV: ${NODE_ENV}
  #      PORT: 3000
  #      FRONTEND_URL: ${FRONTEND_URL}
  #    ports:
  #      - "${BACKEND_PORT}:3000"
  #    depends_on:
  #      postgres:
  #        condition: service_healthy
  #      redis:
  #        condition: service_healthy
  #    networks:
  #      - horarios_network
  #    healthcheck:
  #      test:
  #        [
  #          "CMD-SHELL",
  #          'node -e "require(''net'').createConnection({port:3000},function(){process.exit(0)}).on(''error'',function(){process.exit(1)})"',
  #        ]
  #      interval: 30s
  #      timeout: 5s
  #      retries: 5
  #      start_period: 40s

  #  frontend:
  #    build:
  #      context: ./frontend
  #      dockerfile: Dockerfile
  ##    container_name: ${FRONTEND_CONTAINER_NAME}
  #    depends_on:
  #      backend:
  #        condition: service_healthy
  #    ports:
  #      - "${FRONTEND_PORT}:80"
  #    networks:
  #      - horarios_network
  #    healthcheck:
  #      test:
  #        [
  #          "CMD-SHELL",
  ##          "wget --no-verbose --tries=1 --spider http://localhost/health || exit 1",
  #        ]
  #      interval: 30s
  #      timeout: 5s
  #      retries: 5
  #      start_period: 10s

  # Servidor SMTP para desarrollo - captura emails localmente
  mailhog:
    image: mailhog/mailhog:latest
    container_name: horarios_mailhog
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI
    networks:
      - horarios_network

networks:
  horarios_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  pgadmin_data:
